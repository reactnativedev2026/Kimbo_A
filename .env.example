# =========================================================================
# Kimbo AI Backend Environment Configuration Template
# =========================================================================
# Copy this file to '.env' in the same directory and configure the values.
# Do NOT commit the '.env' file containing real secrets to version control.

# -------------------------------------------------------------------------
# Database Configuration
# -------------------------------------------------------------------------
# Default local database (SQLite)
DATABASE_URL=postgresql://postgres:BALAJI@5566s@db.njqnncmialsablnbjcgs.supabase.co:5432/postgres

# To connect to Supabase (PostgreSQL), uncomment the line below and replace 
# [PASSWORD] and [PROJECT_ID] with your actual Supabase credentials:
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT_ID].supabase.co:5432/postgres

# To connect to Hostinger PostgreSQL (if migrated in the future), configure:
# DATABASE_URL=postgresql://[DB_USER]:[DB_PASSWORD]@[DB_HOST]:5432/[DB_NAME]
